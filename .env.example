# Copia este archivo a `.env` y completa tu API key.
# La key se obtiene en https://platform.claude.com/ (sección API Keys).
ANTHROPIC_API_KEY=sk-ant-...

# Modelo por defecto del agente (configurable sin tocar código).
MODEL=claude-sonnet-4-6
